pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        maven { url "https://api.xposed.info/" }
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://api.xposed.info/" }
    }
}
rootProject.name = "ExtClipboardManager"
include ':app'
